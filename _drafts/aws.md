* Create User "uploader"
* create group "writers" --> power user access
* add uploader to writers
* edit permission on bucket --> publicly visible
* node plugins did not work, aws cli does. go figure
* `aws s3 sync` ist ganz cool